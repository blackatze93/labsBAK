{% extends 'base.html.twig' %}

{% block body %}
    <h1>Proyectocurricular</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proyectoCurricular.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ proyectoCurricular.nombre }}</td>
            </tr>
            <tr>
                <th>Facultad</th>
                <td>{{ proyectoCurricular.facultad }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('proyectocurricular_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('proyectocurricular_edit', { 'id': proyectoCurricular.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
