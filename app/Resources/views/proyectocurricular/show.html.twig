{% extends 'base.html.twig' %}

{% block title %}Consultar Proyecto Curricular{% endblock %}
{% block encabezado %}Consultar Proyecto Curricular{% endblock %}

{% block article %}
    <div class="table-responsive">
        <table class="table table-hover">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proyectocurricular.id }}</td>
            </tr>
            <tr>
                <th>{{ "Nombre"|trans }}</th>
                <td>{{ proyectocurricular.nombre }}</td>
            </tr>
            <tr>
                <th>Facultad</th>
                <td>{{ proyectocurricular.facultad }}</td>
            </tr>
            </tbody>
        </table>
    </div>
    <div class="text-center">
        {{ form_start(delete_form) }}
        <a href="{{ path('proyectocurricular_edit', { 'id' : proyectocurricular.id }) }}" title="Editar" class="btn btn-warning">
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            Editar
        </a>
        <button type="submit" onclick="return confirm('¿Está seguro?')" class="btn btn-danger">
            <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
            Eliminar
        </button>
        <a href="{{ path('proyectocurricular_index') }}" class="btn btn-primary">
            <span class="glyphicon glyphicon-th-list" aria-hidden="true"></span>
            Lista
        </a>
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
